server.address=0.0.0.0
server.port=${PORT:9191}

spring.application.name=FrontendVighnesh
spring.thymeleaf.cache=false

# ============ BACKEND CONNECTION ============
# Frontend talks to backend via this URL
hrms.backend.base-url=${BACKEND_URL:http://localhost:9090}

# ============ LOGGING ============
logging.level.org.springframework.web=INFO
logging.level.com.vighnesh.frontend=DEBUG